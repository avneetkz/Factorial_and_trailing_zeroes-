# In this code we have used iterative approach for finding the factorial 

num= int(input("Enter the number you want factorial of: "))

count=1
i= 1

# while loop starts 
while(i<=num):
    count *= i
    i += 1
    
print(f'The factorial of {num} is {count}')
